# Semáforo Timer v5

`config` no canto superior direito e `sync` logo abaixo.
A configuração tem um único campo: período em minutos.
Predefinição: 3 minutos por estado (3 min verde + 3 min vermelho).
A sincronização fica guardada e o ciclo é calculado pelo relógio.
